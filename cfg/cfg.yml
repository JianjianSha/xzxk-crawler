TASKS:
  zj.nb:    # module name of crawler
    ACTIVE: 0
    TYPE: 'cluster.slave'  # standalone, cluster.master, cluster.slave

  gd.sz:
    ACTIVE: 0
  
  court.ws:
    ACTIVE: 0
    TYPE: 'cluster.master'

  court.tyc_ws:
    ACTIVE: 1
    TYPE: 'cluster.slave'